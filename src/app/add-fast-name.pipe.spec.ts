import { AddFastNamePipe } from './add-fast-name.pipe';

describe('AddFastNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AddFastNamePipe();
    expect(pipe).toBeTruthy();
  });
});
